import PluginConfigOptions = Cypress.PluginConfigOptions;
import PluginEvents = Cypress.PluginEvents;
import { Region } from "@saucelabs/testcomposer";
export { Region };
export interface Options {
    region?: Region;
    build?: string;
    tags?: string[];
}
/**
 * Converts the results of a cypress run (the result from `after:run` or `cypress.run()`) to a sauce test report.
 *
 * @param results cypress run results, either from `after:run` or `cypress.run()`
 * @returns {TestRun}
 */
export declare function afterRunTestReport(results: CypressCommandLine.CypressRunResult): Promise<import("@saucelabs/sauce-json-reporter").TestRun>;
export default function (on: PluginEvents, config: PluginConfigOptions, opts?: Options): PluginConfigOptions;
//# sourceMappingURL=index.d.ts.map